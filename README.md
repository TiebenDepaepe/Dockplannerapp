
  # Dock Planner App

  This is a code bundle for Dock Planner App. The original project is available at https://www.figma.com/design/6AFECVi1CdIAOzE4vLj9u4/Dock-Planner-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  